schemaVersion: 1
recipe: file-drop-studio
chrome:
  navVariant: minimal
  footerVariant: simple
home:
  blocks:
    - type: hero
      variant: upload
      props:
        eyebrow: Convertidor PNG
        badges: [Sin subir archivos, Sin instalación, Descarga inmediata]
    - type: tool
      variant: dropzone
    - type: howItWorks
      variant: three-steps
      props:
        eyebrow: Cómo funciona
        title: Cómo convertir una imagen a PNG
        steps:
          - Sube o arrastra una imagen compatible.
          - Espera a que el navegador genere el archivo PNG.
          - Descarga el resultado y úsalo donde lo necesites.
    - type: content
      variant: card
    - type: faq
      variant: split
